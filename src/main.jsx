import React from "react";
import ReactDOM from "react-dom/client";
import ExifFrameApp from "./ExifFrameApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ExifFrameApp />
  </React.StrictMode>
);
