# EXIF Databack — 프로젝트 인수인계 (Claude Code 세션용)

이 문서는 Claude 챗 세션에서 여기까지 만든 맥락을 Claude Code로 이어가기 위한 인수인계 노트다.
GUYSTYLE이 인스타 스토리에 올릴 사진에 **필름카메라 데이터백 스타일 날짜 각인**을 넣는 웹앱.

---

## 무엇을 만드는가

사진을 올리면 EXIF에서 촬영 정보를 뽑아, 사진 위에 프레임/데이터백 각인을 얹어 저장하는 웹앱.
핵심 용도는 **인스타 스토리에 올릴 사진의 데이터백 각인**. (별도 AR 필터가 아님 — Meta Spark는 2025년 종료됨)

배포 대상: **Vercel** (실제 모바일 브라우저에서 저장/회전 검증이 필요하기 때문. 클로드 아티팩트 iframe 샌드박스에서는 파일 저장이 제대로 안 됨.)

---

## 시장 판단 (이미 검토 완료)

- EXIF 프레임/워터마크 앱은 이미 포화 시장 (PhotoFramify, OneLine, MarkFrame 등 다수).
- 데이터백/데이트스탬프도 포화 (Filmlike Date Stamp 10종, Timestamp Camera 26종+).
- 결론: **대중 서비스보다는 GUYSTYLE 본인 스토리 워크플로우용 개인 도구**로 포지셔닝하는 게 현실적.
- 수요 자체는 TikTok 튜토리얼 등에서 꾸준히 확인됨.

---

## 현재 구조

```
exif-databack/
├── index.html            # 엔트리 (viewport-fit=cover, 다크 배경)
├── src/
│   ├── main.jsx          # React 마운트
│   └── ExifFrameApp.jsx  # 전체 앱 (단일 컴포넌트, 외부 EXIF 라이브러리 없음)
├── package.json          # vite + react + lucide-react
├── vite.config.js
├── vercel.json           # SPA rewrite
└── CLAUDE.md             # 이 문서
```

로컬 실행: `npm install && npm run dev`
빌드: `npm run build` (검증 완료, 통과함)
배포: `vercel` (또는 GitHub 연동 후 자동 배포)

---

## 핵심 기능과 구현 세부

### EXIF 파서 (`parseExif`)
- 외부 라이브러리 없이 JPEG APP1 세그먼트를 직접 파싱.
- Make, Model, Orientation, LensMake/LensModel/LensSpecification, ExposureTime, FNumber, ISO,
  FocalLength, DateTimeOriginal 추출.
- GPS는 프라이버시상 의도적으로 안 뽑음.
- **렌즈 정보 결정 순서** (`lensOf`, 2026-09): LensModel(0xa434) → 값이 없거나 `----`/`unknown`
  같은 플레이스홀더면(`LENS_JUNK`) LensSpecification(0xa432)을 `"24-70mm F2.8-4"` 꼴로 조립
  (`fmtLensSpec`) → LensMake(0xa433)가 있으면 앞에 붙임. 셋 다 없으면 빈칸.
  - LensSpecification은 rational 4개짜리라 `readIFD`가 다중 컴포넌트 rational을 **배열**로
    반환하도록 고쳤다. (기존에 읽던 태그들은 전부 count=1이라 영향 없음)
  - **한계**: 제조사 makernote에만 렌즈명을 넣는 바디(캐논 일부 등)는 여전히 못 읽는다.
    makernote는 벤더마다 포맷이 달라서 파서를 따로 써야 함. 일체형 렌즈 컴팩트는 애초에
    렌즈 태그가 없는 게 정상 — 이 경우는 LENS 칸에 직접 입력.
- **검증됨**: Canon PowerShot V1 원본에서 Python PIL과 동일한 값 확인.
  - 예: Make=Canon, Model=Canon PowerShot V1, 1/1600s, f/5, ISO100, 12mm, 2026:06:27.
  - LensModel은 렌즈 일체형이라 원래 비어있음 (파서 문제 아님).

### 프레임 스타일 5종 (`STYLES`)
- **film**: 진짜 35mm 필름 스트립 — 상하 스프로킷 홀 + 주황 엣지 프린팅(카메라명/프레임번호/설정/날짜). `drawFilmStrip`.
- **polaroid**: 크림색 하단 여백 + 텍스트 블록
- **databack**: 사진에 직접 각인 (아래 상세)
- **lcd**: Canon 상단 LCD 모방 — 앰버 반사형 패널, 직립 7세그, AWB 박스/측광 아이콘/노출보정 스케일. `drawLcdPanel`.
- **strap**: 흰 카드 + 하단 정보 바. 좌측 노출정보/촬영시각, 우측에 제조사 로고 · 구분선 · 바디/렌즈명. `drawStrap` (아래 상세).

### 레퍼런스 참고 개선 (exif-frame.yuru.cam 벤치마킹, 2026-07)
- **메타데이터 수동 편집**: 파싱된 EXIF가 편집 가능한 입력(`meta` state)으로 시드됨.
  EXIF 없는 사진(스크린샷/메신저 전달본)도 직접 입력하면 모든 스타일에 반영.
  `metaFromExif` / `exifFromMeta`로 문자열 ↔ EXIF 숫자형 왕복 변환.
- **종횡비 내보내기**: Free / 1:1 / 4:5 / 9:16(인스타 스토리). 크롭 없이 레터박스(`PAD_BG` 색).
  draw()가 오프스크린 content 캔버스에 스타일을 그린 뒤 비율 패딩 합성하는 2단 구조.
- **내보내기 옵션**: JPEG(기본, 품질 슬라이더 60–100, 기본 95) / PNG. 파일명에 스타일 포함.
- **스타일별 UI**: 필드 토글·캡션은 film/polaroid에서만 표시 (strap/databack/lcd는 고정 레이아웃).
- **네이티브 공유**: Web Share API 지원 시 "공유" 버튼 노출 → 파일을 공유 시트로 전달
  (모바일에서 저장 없이 인스타 스토리로 직행). 실패/미지원이면 다운로드 폴백.

### 미학 개선 (2026-07)
- **타이포그래피** (Google Fonts, index.html에서 로드. 미로딩 시 각 스택의 시스템 폴백):
  - `FONT_EDGE`/`FONT_MONO` = Share Tech Mono — 필름 엣지 프린팅·LCD 라벨·UI.
  - `FONT_HAND` = Caveat(라틴) + Nanum Pen Script(한글) — 폴라로이드 손글씨 캡션.
  - JetBrains Mono + Barlow — strap 바 텍스트용(`TEXT_FONTS`). 아래 Strap 섹션 참고.
  - canvas는 리플로우가 없으므로 `document.fonts.load` 완료 시 `fontsReady`로 재렌더.
- **폴라로이드 재디자인** (`drawPolaroid`): 따뜻한 화이트 종이 그라데이션, 하단 친(chin)
  drawW*0.18, 사진 가장자리 헤어라인+상단 그림자(움푹 들어간 느낌), 손글씨 캡션(-0.022rad
  기울임, 캡션 없으면 날짜가 손글씨로), 친 하단에 아주 작은 메타 프린트.
- **필름 스트립 v2**: 엣지 프린트에 같은 색 shadowBlur 이중 드로우(빛 번짐), 상단 우측
  프레임 카운터 `▸ 24 ▸ 24A`, 카메라명 뒤 ISO 병기, 베이스에 세로 그라데이션.
- **FILM GRAIN + VIGNETTE 토글** (`applyFilmLook`): soft-light 노이즈 그레인(alpha 0.4,
  큰 사진은 타일 스케일업) + 라디얼 비네트(0.24). 사진 영역에만 적용, 프레임/각인엔 안 닿음.
  설정에 저장됨(`filmLook`).
- **설정 유지**: 스타일/비율/포맷/품질/필드 토글/로고 선택을 localStorage(`databack:settings`)에 저장.
  업로드한 로고는 용량이 커서 별도 키(`databack:logo`, data URL)에 저장 — 쿼터 초과해도 나머지 설정은 살아남게.
- **모바일 폼 레이아웃** (2026-09 수정): MM/F/SPEED/ISO 2×2 그리드가 아이폰 폭(390px)에서
  컨테이너 밖으로 114px 삐져나가던 문제. 원인은 `1fr 1fr` + **그리드 아이템 기본 `min-width:auto`**
  — 입력의 고유 너비가 컬럼을 밀어냈다. `repeat(2, minmax(0, 1fr))` + MetaField 루트에
  `minWidth: 0`으로 해결. 라벨은 compact 모드(52px)로 줄이고 SHUTTER → SPEED로 축약.
  입력 글자크기는 12→16px: **16px 미만이면 iOS 사파리가 포커스 시 페이지를 확대**한다.
- **기타 UX**: 디코드 중 로딩 표시, 미리보기 아래 출력 해상도 표시, 이미지 로드 후에도
  페이지 아무 데나 드롭해 교체, 클립보드 이미지 붙여넣기(Ctrl+V), 메타 입력 80ms 디바운스.

### Strap 스타일 (2026-09 추가)
레퍼런스(exif-frame 계열 카드)의 구조를 따름. `drawStrap`.

- **레이아웃**: 사진을 `drawW*0.022` 여백 위에 얹고, 하단에 `drawW*0.115` 높이의 정보 바.
  - 좌: `ISO8000 64mm F2.8 1/60s` (볼드) / 아래 `2025/10/12 22:06:32` (회색)
  - 우: [제조사 로고] · 세로 구분선 · [바디명 / 렌즈명] (우측 정렬 2줄)
- **촬영 시각**: 편집용 DATE 입력은 연-월-일만 들고 있어서, 날짜를 안 고쳤으면 원본 EXIF의
  시:분:초를 그대로 붙인다 (`fmtStrapDateTime`). 날짜를 바꾸면 시각은 뗀다.
- **로고**: `public/maker/*.png` = **제조사 공식 로고 아트워크 21종** (브랜드 컬러 그대로,
  투명 배경). 출처와 라이선스는 아래 "로고 에셋 출처" 참고.
  - 우선순위: **업로드한 PNG > 공식 아트워크 > 워드마크 폴백**.
    워드마크(`BRANDS[].text`)는 아트워크가 없는 브랜드(ZEISS/GoPro)와 PNG 로드 실패 시에만.
    캔버스 `letterSpacing` 지원이 들쭉날쭉해서 자간은 글리프 단위로 직접 그린다(`drawTracked`).
  - 아트워크는 필요할 때 `new Image()`로 지연 로드 후 세션 캐시(`logoCache`). 로드가 끝나면
    `logoTick`을 올려 캔버스를 다시 그린다 (캔버스는 스스로 리플로우하지 않으므로).
  - 브랜드는 카메라명에서 자동 감지(`detectBrand`, `BRAND_PATTERNS`). 순서가 중요 —
    LUMIX/OM SYSTEM이 부모 브랜드보다, **PENTAX가 RICOH보다** 먼저 걸려야 한다
    (펜탁스 바디는 Make를 "RICOH IMAGING COMPANY"로 적는다 — 레퍼런스도 같은 예외를 둠).
    모델번호만으로 추정하는 규칙은 오탐이 나서 일부러 안 넣음. 드롭다운에서 수동 지정/끄기 가능.
  - 크기 S/M/L = 바 높이의 0.34/0.46/0.60 (cap height 기준). 단, 로고 폭은 가용폭의 42%를
    넘지 못한다(바디/렌즈명이 잘리는 걸 막기 위함). 바디·렌즈명이 아예 없으면 72%까지 허용.
- **텍스트 폰트 3세트** (`TEXT_FONTS`, strap 전용. 설정에 `textFont`로 저장):
  - `mono` = **JetBrains Mono** (기본). 진짜 코딩 폰트. 모노는 폭이 넓어 크기 0.88배 보정.
  - `terminal` = Share Tech Mono. 앱 UI와 같은 톤, 더 레트로. 단일 웨이트라 볼드는 크기·색으로만 구분.
  - `sans` = Barlow. 레퍼런스(exif-frame)가 쓰는 그로테스크. 원본에 가장 가까운 룩.
  - **로고 워드마크 폴백은 이 설정을 따르지 않는다** — 브랜드명을 모노로 짜면 로고로 안 읽힘.
    항상 `LOGO_SANS`(Helvetica/Arial 계열) 유지. 공식 아트워크는 당연히 영향 없음.
  - UI 버튼 자체가 해당 폰트로 렌더돼서 누르기 전에 자형을 미리 본다.
- **넘침 처리**: 우측 텍스트가 안 들어가면 먼저 폰트를 최대 0.78배까지 줄이고(`squeeze`),
  그래도 넘치면 말줄임(`fitText`). 남는 폭이 바 높이의 0.8배 미만이면 텍스트를 통째로 버리고
  로고만 남긴다.
#### 로고 에셋 출처 (2026-09)
레퍼런스 사이트(exif-frame.yuru.cam)가 로고를 어떻게 처리하는지 직접 확인한 결과:
**공식 로고 PNG를 레포에 그대로 번들**한다 (`web/public/maker/{light,dark}/*.png`, 21개 브랜드,
출처 표기는 따로 없음). 테마 코드는 `web/src/themes/07_STRAP/index.ts`.

- 우리가 쓰는 파일은 그 프로젝트(github.com/yurucam/exif-frame, **GPL-3.0**)의 light 세트를
  복사한 것. `public/maker/`에 21개, 약 960KB.
- **주의**: GPL-3.0은 코드 라이선스고, 로고 자체의 상표권은 각 제조사에 있다. 개인 도구로
  쓰는 동안은 문제 삼을 여지가 적지만, 공개 배포/상업적 사용으로 가면 (1) GPL 의무와
  (2) 상표 사용 두 가지를 따로 따져야 한다. 껄끄러우면 `public/maker`를 비우고 업로드
  방식만 쓰면 된다 — 파일이 없으면 자동으로 워드마크 폴백으로 떨어진다.
- 레퍼런스 구현과의 차이: 저쪽은 light/dark 두 세트(다크모드 옵션)를 두고, 로고 높이를
  `FONT_SIZE*2`(바 높이의 약 0.47)에 폭 상한 400px 고정. 우리는 흰 카드만이라 light 세트만
  쓰고, 크기는 S/M/L(바 높이 0.34/0.46/0.60) + 폭 상한을 가용폭 비율로 잡았다.

- **검증됨(헤드리스 크로미움)**: 합성 EXIF(SONY ILCE-7M4 / ISO8000 / 64mm / F2.8 / 1/60 /
  2025:10:12 22:06:32 / 시그마 28-70 렌즈명)로 가로·세로 사진, 수동입력, 업로드 로고,
  로고 없음, 빈 메타, 9:16 내보내기까지 렌더 확인. 폰트 3세트도 실제 Google Fonts를 받아
  자형까지 확인함 (JetBrains Mono / Barlow / Share Tech Mono 모두 `document.fonts`에 loaded).
  **실기기에서 Google Fonts 로드 실패 시**: mono는 `ui-monospace`/SF Mono로 떨어져 톤 유지,
  sans는 Helvetica로 떨어짐. 로고 워드마크 폴백은 원래 시스템 폰트라 영향 없음.
  - 참고: 이 개발 컨테이너에서 헤드리스 크로미움으로 검증할 때는 에이전트 프록시의 CA를
    크로미움이 모르기 때문에 `ERR_CERT_AUTHORITY_INVALID`로 웹폰트가 죽는다. 네트워크
    차단이 아니라 **CA 신뢰 문제**이고, Playwright 컨텍스트에 `ignoreHTTPSErrors: true`를
    주면 실제 Google Fonts가 정상 로드된다. 배포 환경(Vercel/실기기)과는 무관한 이슈.

### 데이터백 각인 (확정된 튜닝값 — 절대 임의로 바꾸지 말 것)
GUYSTYLE이 레퍼런스 사진(밤 장면, 주황 7세그 각인) 기준으로 반복 조정해 확정한 값:

- **날짜 포맷**: `'26 06 27` (앞 아포스트로피 + 2자리 연도, 공백 구분) — `fmtDatabackDate()`
- **글리프**: 7-세그먼트를 canvas path로 직접 그림 (`SEG`, `drawSegDigit`, `drawSegApos`)
  - `DB_ONE = 0.62` — "1"은 오른쪽 세로획뿐이라 온전한 한 칸을 주면 왼쪽에만 구멍이 생겨
    `10`, `12`의 간격 리듬이 깨진다. 칸을 좁혀 붙인다. (2026-09 수정)
  - `DB_WEIGHT = 0.112` — 세그먼트 두께 / 숫자 높이. 0.13에서 낮춤. 굵으면 뭉쳐 보인다.
  - `DB_GAP = 0.5` — 세그먼트 끝 사이 간격(두께 배수). 0.3에서 올림. 각 획이 분리돼 보여야
    LED 바처럼 읽힌다.
- **이탤릭**: `DB_SLANT = 0.09` (베이스라인 기준 shear. 값 작을수록 더 수직. 0.16→0.09로 세운 게 최종)
- **색**: 글로우 `rgba(255,74,18,*)`, 코어 `rgba(255,120,50,0.95)` (진한 오렌지-레드. 노란기 뺌)
- **크기**: `dh = drawW * 0.024` (작게)
- **위치**: 우측 하단, `pad = drawW * 0.085` 만큼 안쪽으로 인셋
- **자간**: `segMeasure`에서 sep=dh*0.26, 그룹공백 grp=dh*0.75 (넓게)
- **발광 방식**: 그림자 아님. `globalCompositeOperation = "lighter"` (가산 합성)로
  헤일로 3겹(외곽 번짐 `dh*1.1` α.30 / 넓은 헤일로 `dh*0.42` α.45 / 좁은 헤일로 `dh*0.12` α.75)
  을 얹는다. (2026-09: 외곽 번짐 한 겹 추가. 이전엔 2겹이라 필름보다 화면에 가까워 보였다.)
- **적응형 코어** (2026-09): 코어를 2패스로 찍는다.
  1. 불투명 `rgb(255,78,28)`을 일반 합성 — 색을 붙잡아둔다(밝은 사진에서 흰색으로 날아가는 것 방지).
  2. 같은 색을 **가산 합성**으로, 세기 `heat`만큼 덧씌운다.
  `heat = clamp((140 - lum) / 90, 0, 1)`, `lum` = 각인이 얹힐 영역의 평균 휘도(7픽셀마다 샘플).
  어두운 장면이면 1(=가산 포화로 중심이 흰-주황으로 탄다), 밝으면 0(=예전 동작 그대로).
  - **근거**: 실제 데이터백 LED는 광량이 고정이라 밤 장면은 압도하고 낮 장면엔 묻힌다.
    GUYSTYLE 레퍼런스(밤 사진) 실측: 코어 `[243,188,110]`, 최고 휘도 픽셀 `[253,237,212]`(거의 흰색),
    헤일로 `[163,78,43]`. 우리 결과 — 어두움 최고 `[255,156,56]`(레퍼런스 코어와 거리 90),
    중간톤 `[255,133,89]`, 밝음은 코어 `[255,78,28]` 유지.
    참고로 순수 `lighter+lighter`는 거리 134, 개선 전 방식은 372였다.
  - `getImageData`가 실패하면(캔버스 오염 등) `heat = 1`로 둔다.
- **합성 모드 전수 테스트 결과** (2026-09, 9종 비교): 발광체 표현에 쓸 수 있는 건 **가산 계열뿐**이다.
  `multiply`는 빛을 어둡게 만들어 정반대, `overlay`/`soft-light`는 어두운 배경에서 반응이 거의 없고,
  `lighten`/`source-over`는 번짐이 안 생긴다. 후보는 `lighter`(현재) / `screen`(약함, 클리핑 없음) /
  `color-dodge`(가장 강함) 셋. 다시 실험할 일이 생기면 글리프 코드를 소스에서 추출해 하니스에
  붙여 쓸 것 — 재작성하면 실제 렌더와 어긋난다.

원래 Python(Pillow)으로 프로토타이핑한 뒤 canvas로 포팅한 것.
Pillow는 screen 블렌드, canvas는 lighter(가산) 합성 — 원리는 비슷하나 미세한 밝기차 가능.
실제 브라우저에서 글로우가 너무 세거나 약하면 레이어 alpha값(0.45/0.75/0.95) 조정.

---

## 미해결 이슈 (Claude Code에서 최우선으로 볼 것)

### 1. [수정됨] 세로 사진이 가로로 눕는 문제
- 원인: 이중 회전. 디코더가 EXIF Orientation을 이미 적용(픽셀을 세로로)했는데
  draw()가 같은 태그로 또 회전시켜 결국 눕혀짐.
- 수정: `createImageBitmap(file, {imageOrientation:"from-image"})`로 디코드 시점에
  방향을 픽셀에 굽고, draw()의 수동 회전(rotate 6/8/3)과 `orientation` state를 제거.
  `<img>` 폴백도 기본값(from-image)으로 자동 정방향이라 동일하게 동작.
- 검증: Orientation=6 태그를 넣은 JPEG를 새 디코드 경로에 통과시켜 200×400(세로) +
  색 배치(위=빨강, 아래=파랑, 초록 마커=오른쪽 가장자리)로 정방향 확인 완료.
  단, **실기기(특히 iOS Safari)에서 실제 세로 사진으로 최종 확인 권장.**

### 2. [수정됨] 모바일 저장 버튼
- 임시로 넣었던 전체화면 롱프레스 저장 오버레이(`savedUrl`)를 제거.
- 현재 데스크톱/모바일 모두 평범한 `<a download>`(`triggerDownload`) 사용.
- iOS Safari가 실배포에서도 `<a download>`를 무시하면 그때 대응 방식 재논의.

### 3. [알려진 제약] HEIC 미지원
- 아이폰 기본 HEIC는 브라우저가 디코드/EXIF 파싱 불가.
- heic2any(cdnjs 0.0.1)가 있지만 **변환 시 EXIF를 버림** → 촬영 날짜가 사라져 데이터백에 쓸 수 없음.
- 그래서 자동 변환 안 넣음. 대신 HEIC 감지 시 "설정에서 JPEG로 바꾸라"는 안내 메시지.
- Canon PowerShot V1은 원래 JPEG(정확히는 MPO)라 문제없음.

### 4. [처리됨] MPO / 대용량
- PowerShot V1의 .jpeg는 실제로는 MPO(멀티이미지) 포맷 + 22MP(5760×3840).
- MPO는 `<img>`가 디코드 실패할 수 있고, 22MP는 iOS canvas 한도(~16.7MP) 초과.
- 현재: `createImageBitmap`으로 디코드 → 한 변 max 3600px로 자동 축소 → 8.6MP로 정규화.
- EXIF는 항상 원본 바이트에서 먼저 파싱하므로 축소해도 날짜 보존됨.
- 이 로직이 이슈 #1(회전)과 얽혀 있으니 함께 볼 것.

---

## 다음 할 일 제안 순서

1. Vercel 연결 → 실배포 URL 확보. (레포/브랜치는 push돼 있음)
2. 실기기(iOS Safari)에서 세로 사진·저장 버튼·9:16 내보내기 최종 검증.
3. 데이터백 글로우 세기 실환경 미세조정.
4. ~~databack 스타일일 때 필드토글/캡션 숨기기~~ → 완료 (film/polaroid에서만 표시).

## UI 카피 원칙 (2026-09 정리)
화면에 보이는 글은 **제품 카피**지 구현 노트가 아니다. 개발 중 맥락이 그대로 남는 걸
(instruction leak) 한 번 싹 걷어냈으니 다시 넣지 말 것:

- 왜 그렇게 만들었는지 설명하지 않는다. (구 예시: "제조사 공식 로고를 씁니다 (ZEISS·GoPro는
  아트워크가 없어 워드마크로 대체)", "로고 워드마크는 폰트 설정과 무관하게 유지돼요")
- 헤더는 지시문이 아니라 이름이다. (구 예시: "사진에서 촬영 정보를 꺼내 프레임을 태우세요"
  → `DATABACK`)
- 섹션 라벨에 부연을 붙이지 않는다. (`RATIO — 9:16 = 인스타 스토리` → `RATIO`,
  `METADATA (수정 가능)` → `METADATA`)
- 에러는 **다음에 뭘 할지**만 말한다. 내부 원인(MPO, canvas 한도, EXIF 유실 등)은 빼고
  사용자가 취할 행동만 남긴다.
- 라벨의 괄호 주석(`(선택)`, `(워드마크)`)은 쓰지 않는다. placeholder는 예시값 그 자체로 둔다.

## 하지 말 것
- 확정된 데이터백 튜닝값(SLANT 0.09, 색, 크기 0.024, pad 0.085, 그룹 자간)을 임의로 바꾸지 말 것.
  2026-09에 글리프 품질만 손봤다(`DB_ONE`/`DB_WEIGHT`/`DB_GAP` + 번짐 한 겹). 위 5개 값은
  그때도 건드리지 않았다. 다음에 조정할 일이 생기면 **변형을 여러 개 빌드해 크롭 비교 시트를
  만들어 고르게 할 것** — 눈으로 안 보고 숫자만 바꾸지 말 것.
- 회전 로직을 실제 사진 확인 없이 추측으로 수정하지 말 것.
- HEIC 자동 변환 넣지 말 것 (EXIF 유실됨).
